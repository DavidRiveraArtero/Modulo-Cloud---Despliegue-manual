import * as React from 'react';
import { AppLayout } from '#/layouts';
import { RickCharacterContainer } from '#pods/rick-character/rick-character.container.js';

export const RickCharacterScene = () => (
    <AppLayout>
        <RickCharacterContainer/>
    </AppLayout>
);
